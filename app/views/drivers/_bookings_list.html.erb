<% bookings.each do |b| %>
  <p><strong>  <%= b.date %></strong> at <strong><%= b.time.strftime("%I:%M")  %></strong><br>
    <strong><%= b.make %> <%= b.model %></strong><br>
    <i style="font-style: italic"><%= b.description %></i> 
  </p>
  <%= link_to "Cancel",driver_booking_path(b.id), method: :delete, class: "btn btn-outline-danger", data: {confirm: "Cancel booking for #{b.date} at #{b.time.strftime('%I:%M')}?", disable_with: "Cancelling..." } %>
  <hr>
<% end %>

<div style="text-align: center">
  <% if bookings.count > 2  %>
    <%= link_to "See Older",driver_bookings_path(@driver.id), class: "btn btn-outline" %>  
  <% end %>
</div>

          